diff --git a/src/core/ai/dims.ts b/src/core/ai/dims.ts
--- a/src/core/ai/dims.ts
+++ b/src/core/ai/dims.ts
@@ -201 +201,2 @@
-      // Anthropic has no embedding model.
+    case 'codex-cli':
+      // Anthropic and Codex CLI have no embedding model.
diff --git a/src/core/ai/gateway.ts b/src/core/ai/gateway.ts
--- a/src/core/ai/gateway.ts
+++ b/src/core/ai/gateway.ts
@@ -2410,0 +2411,4 @@
+    case 'codex-cli': {
+      const { CodexCliLanguageModel } = require('./providers/codex-cli-language-model.ts');
+      return new CodexCliLanguageModel(modelId);
+    }
@@ -2918,0 +2923,4 @@
+    case 'codex-cli': {
+      const { CodexCliLanguageModel } = require('./providers/codex-cli-language-model.ts');
+      return new CodexCliLanguageModel(modelId);
+    }
diff --git a/src/core/ai/recipes/index.ts b/src/core/ai/recipes/index.ts
--- a/src/core/ai/recipes/index.ts
+++ b/src/core/ai/recipes/index.ts
@@ -31,0 +32 @@
+import { codex } from './codex.ts';
@@ -56,0 +58 @@
+  codex,
diff --git a/src/core/ai/types.ts b/src/core/ai/types.ts
--- a/src/core/ai/types.ts
+++ b/src/core/ai/types.ts
@@ -26 +26,2 @@
-  | 'claude-cli';
+  | 'claude-cli'
+  | 'codex-cli';
diff --git a/src/core/ai/recipes/codex.ts b/src/core/ai/recipes/codex.ts
new file mode 100644
--- /dev/null
+++ b/src/core/ai/recipes/codex.ts
@@ -0,0 +1,27 @@
+import type { Recipe } from '../types.ts';
+
+/**
+ * Codex through the locally logged-in `codex` CLI. This is intentionally a
+ * chat-only recipe: GBrain uses it for think and synthesis, while DeepSeek
+ * remains the tool-capable provider for subagents and Dream patterns.
+ */
+export const codex: Recipe = {
+  id: 'codex',
+  name: 'Codex (via CLI)',
+  tier: 'native',
+  implementation: 'codex-cli',
+  auth_env: { required: [] },
+  touchpoints: {
+    chat: {
+      models: ['gpt-5.5', 'gpt-5', 'gpt-4o'],
+      supports_tools: false,
+      supports_subagent_loop: false,
+      supports_prompt_cache: false,
+      max_context_tokens: 200000,
+    },
+  },
+  aliases: { default: 'gpt-5.5' },
+  setup_hint:
+    'Install Codex CLI and log in with your ChatGPT subscription. ' +
+    'Set GBRAIN_CODEX_BIN when the `codex` command is not on PATH.',
+};
diff --git a/src/core/ai/providers/codex-cli-language-model.ts b/src/core/ai/providers/codex-cli-language-model.ts
new file mode 100644
--- /dev/null
+++ b/src/core/ai/providers/codex-cli-language-model.ts
@@ -0,0 +1,66 @@
+import { spawn } from 'node:child_process';
+import { mkdtemp, readFile, rm } from 'node:fs/promises';
+import { tmpdir } from 'node:os';
+import { join } from 'node:path';
+import type { LanguageModelV2, LanguageModelV2CallOptions, LanguageModelV2Content, LanguageModelV2Message, LanguageModelV2Prompt } from '@ai-sdk/provider';
+
+function codexBin(): string { return process.env.GBRAIN_CODEX_BIN ?? 'codex'; }
+
+function renderPrompt(prompt: LanguageModelV2Prompt): string {
+  const rendered: string[] = [];
+  for (const message of prompt as ReadonlyArray<LanguageModelV2Message>) {
+    if (message.role === 'system') { rendered.push(`<system>\n${message.content}\n</system>`); continue; }
+    const content = message.content.map(part => {
+      if (part.type === 'text') return part.text;
+      if (part.type === 'file') return `[file ${part.mediaType ?? 'unknown'} omitted]`;
+      if (part.type === 'tool-call') return `[tool call ${part.toolName}: ${part.input}]`;
+      if (part.type === 'tool-result') return `[tool result ${typeof part.output === 'string' ? part.output : JSON.stringify(part.output)}]`;
+      return '';
+    }).filter(Boolean).join('\n');
+    if (content) rendered.push(`<message role="${message.role}">\n${content}\n</message>`);
+  }
+  return ['You are a non-tool reasoning model inside GBrain. Follow the supplied messages and return only the requested final answer.', ...rendered].join('\n\n');
+}
+
+async function runCodex(prompt: string, model: string, signal?: AbortSignal): Promise<string> {
+  const cwd = await mkdtemp(join(tmpdir(), 'gbrain-codex-'));
+  return new Promise((resolve, reject) => {
+    const outputPath = join(cwd, 'last-message.txt');
+    const child = spawn(codexBin(), ['exec', '--ephemeral', '--model', model, '--sandbox', 'read-only', '--skip-git-repo-check', '--ignore-rules', '--output-last-message', outputPath, '-'], {
+      cwd, env: { ...process.env, GBRAIN_CODEX_BRIDGE_DEPTH: '1' }, stdio: ['pipe', 'pipe', 'pipe'],
+    });
+    let stderr = '';
+    child.stderr.on('data', chunk => { stderr += String(chunk); });
+    const cleanup = async () => { await rm(cwd, { recursive: true, force: true }); };
+    const onAbort = () => { child.kill('SIGTERM'); };
+    if (signal) signal.addEventListener('abort', onAbort, { once: true });
+    child.on('error', async err => {
+      if (signal) signal.removeEventListener('abort', onAbort);
+      await cleanup(); reject(new Error(`codex CLI spawn failed: ${err.message}`));
+    });
+    child.on('close', async code => {
+      if (signal) signal.removeEventListener('abort', onAbort);
+      try {
+        if (code !== 0) throw new Error(`codex exec exited ${code}: ${stderr.trim()}`);
+        const text = (await readFile(outputPath, 'utf8')).trim();
+        if (!text) throw new Error('codex exec returned no final message');
+        resolve(text);
+      } catch (err) { reject(err); } finally { await cleanup(); }
+    });
+    child.stdin.on('error', () => {});
+    child.stdin.end(prompt);
+  });
+}
+
+export class CodexCliLanguageModel implements LanguageModelV2 {
+  readonly specificationVersion = 'v2' as const;
+  readonly provider = 'codex-cli';
+  readonly supportedUrls = {};
+  constructor(readonly modelId: string) {}
+  async doGenerate(options: LanguageModelV2CallOptions): Promise<{ content: LanguageModelV2Content[]; finishReason: 'stop'; usage: { inputTokens: undefined; outputTokens: undefined; totalTokens: undefined }; warnings: never[] }> {
+    if (options.tools?.length) throw new Error('Codex CLI provider does not support GBrain tool calls; route this task to DeepSeek.');
+    const text = await runCodex(renderPrompt(options.prompt), this.modelId, options.abortSignal);
+    return { content: [{ type: 'text', text }], finishReason: 'stop', usage: { inputTokens: undefined, outputTokens: undefined, totalTokens: undefined }, warnings: [] };
+  }
+  async doStream(): Promise<never> { throw new Error('Codex CLI LanguageModel does not support streaming.'); }
+}
